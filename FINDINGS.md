# CEB 格式逆向结论

全部结论基于 `samples/ceb/` 下的真实样本（国网公文，490 KB，17 页）实测，
并与 <https://github.com/Chingliu/ceb2pdf>（C++，无许可证声明，2021-08 停更）交叉验证。

## 一句话

**CEB 不是一种独立的版式格式，而是一个被两层弱加密包裹的标准 PDF。**
这意味着转换是**无损**的：不需要渲染引擎、不需要字体处理、不需要 OCR，
输出的就是原始 PDF 本身。

## 容器

```
0x00  "Founder CEB\0"      12 B 魔数
0x0C  u16 = 0
0x0E  u16 = 3              版本
0x10  u32 = 102            段表字节数 (= 段数 × 17)
0x14  u16 = 6              段数
0x16  u16 = 1
0x18  7 B 保留
0x1F  段表，每项 17 字节： off u32 | len u32 | type u8 | 8 B 保留
```

段类型（本样本）：

| type | 长度 | 内容 |
|-----:|-----:|------|
| 3 | 498315 | PDF 主体 |
| 4 | 16 | RC4 密钥 |
| 5 | 64 | RSA 包裹的 3DES 密钥 |
| 16 | 4 | 算法 ID，`0x80000002`；高位=密钥被 RSA 包裹 |
| 255 | 150 | 制作信息（`Maker 3.2`、`20140527`、`Windows XP`） |
| 0 | 1403 | GBK 编码的 XML 元数据（`<MergerFile>`） |

各段首尾相接，最后一段正好结束于 EOF。

## 第一层：Founder-RC4（作用于整个 PDF 主体）

密钥 = 段 4（16 字节）。每 **64 KiB** 从初始状态重置一次。

KSA 前把每个密钥字节 `|= 0xAA`。PRGA 有一处实现错误：

```c
byT = pbyState[byX];
pbyState[byX] = pbyState[byY];
pbyState[byY] = pbyState[byT];   // 应为 = byT，这里用值当下标
```

用**值**而非暂存变量赋值，破坏了置换的双射性，状态迅速坍缩。
实测密钥流分布：`0x45` 占 **97.2%**，`0x5e` 0.6%，`0xfa` 0.2%。

这就是为什么整个文件"看起来像是 XOR 0x45"——那不是设计，是 bug 的副作用。
**不能用 XOR 0x45 近似**：开头约 1.8 KB（KSA 刚结束、尚未坍缩）偏差最大。

## 第二层：3DES-OFB（逐个 PDF stream）

密钥获取：对段 5 的 64 字节做无填充 RSA 模幂，模数/指数是源码里硬编码的
两个 512-bit 常量（按**小端** BIGNUM limb 解释）。结果前 4 字节是长度（=24），
其后即 24 字节 3DES 密钥。

本样本：`a1e7f815671f82812870e2315352f156 9a4cb39584f08b03`
——**前 16 字节与 RC4 密钥完全相同**，可作交叉校验。

模式 **`3DES-CFB`、段长 64 位**，`iv = key[:8]`，**每 256 字节重新初始化**。
作用范围是 `stream` 关键字后到 `endstream` 之间。

> **曾经判断错过**：一度认定是 OFB。CFB64 与 OFB 在每个 chunk 的**前 8 字节
> 数学上恒等**（`P1 = C1 ⊕ E(IV)`），且明文全零时两条链完全重合 —— 所以
> 「zlib 头解出 `78 9c`」和「空白位图解出全零」都**无法**区分两者。
> 段 16 的算法 ID 低位 `2` = `TYPE_3DES_CFB` 从一开始就是对的，文件自己写着。
>
> **模式由算法 ID 低位分派**（18 个样本实测，两条独立路线交叉验证）：
>
> | `algo & 0xF` | 模式 | 样本数 |
> |---|---|---|
> | `0` | 流不加密，整段跳过 | 1 |
> | `1` | 3DES-**OFB** | **13** |
> | `2` | 3DES-**CFB**，段长 64 位 | 4 |
>
> 硬编码 CFB64 会解错 13 个样本 —— 多数派其实是 OFB。

因为 OFB 是流密码、长度不变，可以**原地解密**，PDF 的 xref 偏移全部保持有效。
最后把 trailer 里的 `/Encrypt N 0 R` 用等长空格抹掉即可。

## 验证状态

| 环节 | 状态 | 证据 |
|------|------|------|
| 容器解析 | ✅ | 段首尾相接且正好到 EOF |
| RC4 层 | ✅ | 输出 `%PDF-1.5`；xref 表、对象字典、`endstream\rendobj\r6 0 obj\r1139` 全部为干净文本 |
| RSA 解包 | ✅ | 长度字段正好 24；前 16 字节 == RC4 密钥 |
| 3DES 层 | ✅ | `/FontFile2` 声明 `/Length1 11553`，解压后**恰好** 11553 字节（文件自带校验位）；字形位图渲染为清晰笔画而非噪点 |
| PDF 重建 | ✅ | 产出合法 PDF，xref 有效 |
| 页面内容流 | ✅ | 样本1：20/20 inflate、18 页渲染、抽出正确 GBK 正文；样本2：769 流 inflate |

## 容器变体（两个样本的差异）

不能假定头部是固定的 12 字节魔数 + u16 版本：

| | 样本 1 | 样本 2 |
|---|---|---|
| 魔数第 12 字节 | `\0` | **空格** |
| 版本串（0x0C 起，NUL 结尾） | 空 | `2.99D` |
| 段数 | 6 | 4 |
| 段 5 | 64 B（RSA 包裹） | **24 B（裸 3DES 密钥）** |
| 算法 ID | `0x80000002` | **`0`** |
| 流加密 | 3DES-CFB64 | **无**（仅 RC4） |

解析要点（18 个样本验证）：
- 魔数**只比对前 11 字节** `Founder CEB` —— 14/18 的第 12 字节是空格而非 `\0`
- `0x0C` 起是 NUL 结尾的版本串（空 / `2.50f` / `2.50w` / `2.50U` / `2.99D`）
- **`0x10` 不是段表字节数** —— 那个偏移落在版本串内部。`'f'`=102=6×17、
  `'D'`=68=4×17 只是巧合，别校验它
- 只用 `0x14` u16 段数 + `0x1F` 起 17 字节/项；跳过 `len==0` 或越界的条目
- **段类型字节不可盲信**：16/18 的主体是 type **2** 而非 3，且常伴随一个
  零长度的 type-3 诱饵；算法 ID 段见过 type 16/0/34；末项 type 可能是
  未初始化内存残留。改为**按长度形状识别**：

  | 角色 | 判据 |
  |---|---|
  | 主体 | 最长的段 |
  | RC4 密钥 | 长度 16 |
  | 对称密钥 | 长度 24（裸）或 64（RSA 包裹） |
  | 算法 ID | 长度 4 |

- 主体偏移不固定（见过 90 和 1722），一律从段表读

## 保真度限制：字体

部分 CEB **不内嵌字体**，只按名引用方正字库（方正仿宋_GBK、方正黑体_GBK 等），
且无 `ToUnicode` CMap。18 个样本里有 5 个是这种情况。

这类文件在未安装方正字库的机器上会发生字体替换。最明显的症状是方正的
「数码字体」：`〔2013〕20号` 会显示成 `〔圆园员猿〕圆园号` —— 数字字形挂在
圆/园/员/猿 等汉字的编码上，替换字体后就露馅了。

**这不是转换损失** —— CEB 容器里本来就没有那些字体，Apabi Reader 显示正确
是因为它自带字库。转换器不应试图修复它，但应在 verbose 模式下提示。

## 其它线索

- `libCeb2Pdf.so`：来自 `com.founderinternational.cebreader` 1.0.20200629-1
  （**方正国际**，不是方正阿帕比），带 UOS 代码签名证书，经信创渠道正式发行。
  2.5 MB，x86-64 only，无 QtGui/X11 依赖，仅导出 `c2p_ceb2pdf` /
  `c2p_getLastError` / `c2p_getVersion` 三个符号。
  该包**未附带任何 EULA**，遍历全部二进制未找到禁止逆向的条款 —— 注意这是
  「未找到禁令」而非「明示授权」。**再分发不被允许**（保留所有权利），
  本项目只做互操作性分析、不分发其任何二进制。
  其 `.rodata` 中原样含有本项目使用的 `KEY_N`/`KEY_E` 常量，可佐证这两个
  常量确为方正自有。**最终未对该库反汇编** —— 密码分析已足够，没有必要。
- CEBX 是另一种格式，魔数 `@XDA`，不是 ZIP。本项目暂不涉及。
- 其它项目：`hhtczengjing/ceb2pdf`（53★ MIT，仅 Windows 二进制无源码）、
  `weimingtom/cebxconvert`（调用方正 SDK）、`qinlili23333/ApabiDownloader`（JS）。

## 范围声明

本样本的"加密"密钥完整存放在文件自身内，无授权服务器、无用户凭证、
无许可证绑定——属于格式层面的混淆，而非针对特定用户的 DRM。
工具应当**检测并拒绝**真正带授权绑定的文件（如图书馆借阅本）。
