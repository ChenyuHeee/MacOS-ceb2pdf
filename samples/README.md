# 测试样本

样本文件本身不入库（第三方公文，见 `.gitignore`）。
测试通过 SHA-256 对齐，把对应文件放到本目录即可。

| 文件 | SHA-256 | 说明 |
|------|---------|------|
| `ceb/gw-2014-526.ceb` | `e913c6b13609c81215ed59a982f457f575ce42edcd7065dae558d5ec58ce63f7` | 国网公文，490 KB，17 页，version=3，algo=0x80000002 |

需要更多样本 —— 不同容器版本、不同算法 ID、无 RSA 包裹的、以及带授权绑定的
（后者用于验证工具能正确**拒绝**，而不是静默产出坏文件）。
